# PlatformIO build artifacts
firmware/.pio/
firmware/.vscode/

# Per-device secrets (WiFi creds, future tokens). secrets.h.example is tracked.
firmware/src/secrets.h

# OS
.DS_Store

# Claude Code
.claude/

# Node tooling deps (npm install --no-save)
tools/node_modules/
tools/package-lock.json

# Mac daemon venv
daemon/.venv/

# Python bytecode cache
__pycache__/
*.pyc
/firmware/src/seen_logo_frames.h
