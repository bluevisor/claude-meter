#pragma once

// Copy this file to firmware/src/secrets.h to override the build-time
// WiFi credentials baked into wifi_cfg.cpp. Anything not defined here
// falls back to the defaults in wifi_cfg.cpp.
//
// On the Waveshare LCD-1.3 build WiFi is provisioned but idle — the
// daemon link is BLE — so these are forward-looking for OTA/NTP/etc.

// #define WIFI_SSID     "your-ssid"
// #define WIFI_PASSWORD "your-password"
